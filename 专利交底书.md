# 专利交底书

## 发明名称
一种融合 DOM 语义解析与智能决策的测试自动化执行方法及系统

---

## 1. 发明目的及基本方案

### 发明目的
本发明旨在解决现有 Web 应用自动化测试技术中脚本维护成本高、适应性差、测试覆盖有限以及对测试人员编程能力要求高等问题，提供一种无需预先编写测试脚本、能够自动理解页面结构、智能决策测试操作、并自动生成可维护测试脚本的测试自动化执行方法及系统。

### 基本方案
本发明提供一种融合 DOM（文档对象模型）语义解析与智能决策的测试自动化执行方法，其核心思路是：

1. **页面语义化**：加载目标网页后，遍历 DOM 树结构，为每个可见元素提取属性信息并分配唯一的引用标识，建立 DOM 元素与语义化引用之间的映射关系，生成页面语义表示快照；

2. **任务意图解析**：接收用户以自然语言描述的测试任务，通过语义分析提取关键实体和操作意图；

3. **推理 - 行动循环执行**：采用 ReAct（Reasoning-Acting）架构，将当前页面语义表示和对话历史构建为提示消息，调用大语言模型生成推理响应，解析后执行相应的页面操作，获取观察结果并进入下一轮循环，直至任务完成；

4. **脚本自动生成**：将整个执行过程中记录的操作序列进行后处理，生成标准化的可重放测试脚本，同时输出执行日志和前端接口性能分析报告。

本发明还提供一种测试自动化执行系统，包括输入模块、处理模块、执行模块、输出模块和网络监控模块，用于实现上述方法的各个步骤。

---

## 2. 相关术语解释

| 术语 | 解释 |
|------|------|
| **DOM（Document Object Model，文档对象模型）** | 浏览器中网页的结构化表示，以树形结构组织 HTML 元素，每个元素称为节点，可通过编程方式访问和操作。 |
| **语义解析** | 将 DOM 树中的元素转换为具有语义信息的抽象表示的过程，包括提取元素类型、文本内容、层级路径、可操作状态等属性，并为每个元素分配唯一的引用标识。 |
| **ReAct 架构（Reasoning-Acting，推理 - 行动）** | 一种人工智能任务执行范式，通过"推理→行动→观察"的交替循环逐步完成任务：先分析当前状态并推理下一步行动，执行行动后获取观察结果，再基于新状态进行下一轮推理。 |
| **页面语义表示快照** | 在某一时刻对页面所有可操作元素的引用标识、属性信息及元素间关系的完整记录，用于后续操作生成和状态比较。 |
| **引用标识** | 为页面中每个可操作元素分配的唯一标识符，用于替代传统的 XPath 或 CSS 选择器，使测试脚本不依赖于具体的 DOM 路径。 |
| **大语言模型** | 基于深度学习的大规模预训练语言模型，能够理解自然语言指令并生成结构化的推理响应，包括思考、动作和动作输入等字段。 |
| **上下文压缩** | 在长时间任务执行过程中，对对话历史记录进行摘要压缩，保留最近 N 轮对话和关键信息，以减少 token 消耗并支持更长时间的任务执行。 |
| **操作 - 请求对应关系表** | 将测试执行过程中的每个操作与其触发的网络请求进行关联存储的数据结构，用于生成前端接口性能分析报告。 |
| **MCP（Model Context Protocol，模型上下文协议）** | 一种用于扩展大语言模型工具能力的协议，允许通过配置文件接入外部工具服务，无需修改代码即可扩展智能体能力。 |

---

## 3. 技术背景

### 3.1 软件测试的发展历程

软件测试作为软件质量保障的重要环节，其发展经历了从纯手工到自动化、从低效到高效的演进过程。

**第一阶段：纯手工测试（1970s-1990s）**
早期的软件测试完全依赖人工执行，测试人员根据测试用例手动操作软件界面，记录实际结果并与预期结果比对。这种方式的主要局限在于：
- 测试效率低，每次回归测试都需要重复大量人工操作
- 容易遗漏测试步骤，测试结果的一致性难以保证
- 人力成本高，大型项目需要庞大的测试团队

**第二阶段：录制 - 回放自动化（1990s-2000s）**
随着图形用户界面的普及，出现了 Selenium IDE、UI.Vision 等录制 - 回放工具。测试人员通过浏览器插件记录操作步骤，生成可重复执行的测试脚本。这种方式的进步在于实现了测试执行的自动化，但存在明显缺陷：
- 生成的脚本缺乏灵活性，页面元素微小变化即导致脚本失效
- 无法处理复杂的业务逻辑和条件分支
- 脚本维护成本高，需要人工介入修改

**第三阶段：基于选择器的脚本自动化（2000s-2010s）**
以 Selenium WebDriver 为代表的第二代自动化测试框架兴起，测试人员通过编写代码，使用 XPath、CSS 选择器定位元素，执行点击、输入等操作。这种方式的优势是脚本表达能力强，可处理复杂场景，但问题在于：
- 对页面结构变化高度敏感，元素路径变更需要重新定位
- 需要测试人员具备编程能力，门槛较高
- 大型应用的脚本维护成为沉重负担

**第四阶段：基于图像识别的自动化（2010s 至今）**
Sikuli、Eggplant 等工具采用图像匹配技术定位元素，不依赖 DOM 结构，可测试任何可见界面。然而这种方式存在新的局限：
- 执行速度慢，图像匹配计算开销大
- 对屏幕分辨率和界面缩放比例敏感
- 难以处理动态内容和变化场景

**第五阶段：基于机器学习的测试方法（2015s 至今）**
近年来，一些研究开始探索将机器学习技术应用于测试自动化，如通过训练模型识别页面元素类型、预测用户操作等。这类方法在一定程度上提高了适应性，但通常需要大量标注数据进行训练，泛化能力有限。

### 3.2 当前技术领域面临的挑战

随着 Web 应用的快速迭代和复杂性不断增加，现有自动化测试技术面临以下共同挑战：

1. **脚本维护成本高**：无论是录制回放还是基于选择器的方法，都需要在页面结构变化时手动更新脚本。对于大型 Web 应用，这是一项繁重的工作。

2. **适应性差**：现有方法难以应对动态生成的页面内容、A/B 测试导致的界面差异、以及跨浏览器兼容性问题。

3. **测试覆盖有限**：由于脚本编写和维护的成本限制，很多边缘场景和异常流程难以被充分测试。

4. **对测试人员技能要求高**：编写和维护自动化测试脚本需要编程能力和对测试框架的深入理解，这限制了自动化测试的普及。

5. **缺乏性能分析能力**：现有测试方法主要关注功能验证，难以同时捕获和分析前端接口的性能数据。

因此，亟需一种能够自动理解页面结构、智能决策测试操作、并生成可维护测试脚本的自动化执行方法及系统，以解决现有技术中存在的上述问题。

---

## 4. 最接近的现有技术

经检索分析，与本申请最接近的现有技术为中国专利申请《基于用例自动化的软件测试方法》（以下简称"对比技术"）。以下对该技术进行详细说明。

### 4.1 技术方案概述

对比技术公开了一种基于用例自动化的软件测试方法，应用于客户端，其核心流程包括以下步骤：

1. **请求文件获取与用例生成**：响应于待测试软件的测试请求，获取测试请求对应的请求文件（HAR 格式），生成请求文件对应的测试用例；

2. **用例参数更新**：对测试用例中的用例参数进行更新，得到更新后测试用例；

3. **用例组合处理**：对更新后测试用例进行组合处理，得到组合测试用例；

4. **用例调试处理**：对组合测试用例进行调试处理，得到调试结果；

5. **分布式执行**：在调试结果表示组合测试用例调试通过的情况下，将组合测试用例发送至服务端，由服务端分配至执行服务器进行测试处理，得到测试结果。

### 4.2 系统架构

对比技术的系统架构如图 1 所示（参考原专利附图），包括：

- **客户端 101**：负责生成组合测试用例，包括用例生成、参数更新、组合处理、调试处理等功能模块
- **服务端 102**：负责对组合测试用例进行调度和分配
- **执行服务器 103**：负责对组合测试用例执行测试，通常调用 JMeter 等测试工具

### 4.3 具体实施方式

对比技术的详细实施流程如下（参考原专利说明书图 4、图 5）：

**步骤 1：接口抓包并导出为 HAR 格式**
通过抓包工具对业务请求进行抓包，导出为 HAR（HTTP Archive）文件格式。HAR 文件记录了请求的完整信息，包括请求头、请求体、请求 URL 等。

**步骤 2：在 API 管理中导入 HAR 文件**
将 HAR 文件通过平台导入功能生成接口测试用例。提供两种导入方式：
- 导入为基础测试用例：适用于单一接口测试
- 导入为场景测试用例：适用于多接口业务流程测试

**步骤 3：参数变量池替换**
导入时检测抓包请求中是否存在参数池中已定义的字段名称，如果有则将导入数据中的字段替换为<字段名>形式。例如：`fdId=18d62a5b35945857` 自动替换为 `fdId=<fdId>`。

**步骤 4：场景测试用例组成**
将导入的接口自动生成场景测试用例，此场景测试用例会包含登录用例与该导入的接口用例。登录用例根据预设置的默认登录用例生成。

**步骤 5：绑定接口变量并调试**
为场景测试用例中多个接口之间的依赖参数进行绑定，例如新建流程依赖登录的 cookie，通过绑定接口变量实现参数传递。

**步骤 6：添加到待执行测试集合**
将调试通过的场景测试用例添加到待执行集合中，通过设定任务触发执行。

**服务端执行流程**：
1. 测试中心服务检测待执行任务（每 5 秒检测）
2. 调度服务将测试用例平均分配给多个执行服务
3. 执行服务调用 JMeter 执行测试
4. 生成在线测试报告

### 4.4 功能模块

对比技术还提供以下功能模块（参考原专利说明书图 6-图 20）：

- **环境管理**：维护被测应用环境和数据库环境的配置信息
- **系统服务**：维护被测系统的相关模块和 API 接口
- **变量管理**：管理全局变量、全局 header、随机变量、接口变量等
- **条件管理**：管理前置条件与后置条件，包括接口条件、数据库条件、延时条件
- **测试管理**：包括用例管理、函数管理、参数替换
- **执行中心**：管理测试集合和集合用例
- **报告中心**：查看执行结果明细
- **调度中心**：管理执行机和调度任务
- **系统管理**：账号管理、角色权限管理、字典管理

---

## 5. 现有技术缺点推导

针对上述最接近的现有技术，本部分以因果关系推理的方式推导其存在的技术缺陷。

### 5.1 依赖预定义参数池导致适应性受限

**推理过程：**
对比技术通过参数变量池实现参数替换（说明书第 [0151] 段），即用预定义的参数名称替换抓包得到的实际参数值。→ 这意味着参数替换的前提是参数池中已存在对应的参数定义。→ 当遇到参数池中未定义的新参数时，系统无法自动识别和处理。→ 需要人工预先在参数池中配置所有可能的参数。→ 对于大型 Web 应用，参数数量庞大且频繁变化，人工维护参数池的工作量巨大。→ **导致测试系统对页面或接口变化的适应性受限，新增参数需要人工介入配置。**

### 5.2 基于固定模板的用例生成方式缺乏灵活性

**推理过程：**
对比技术通过预设测试用例模板生成测试用例（说明书第 [0163]-[0169] 段），根据用例导入方式（基础测试用例或场景测试用例）选择对应的模板。→ 测试用例的结构和字段完全由预定义的模板决定。→ 当测试需求超出模板预设的范围时，例如需要添加新的验证逻辑或特殊的执行步骤，无法通过模板自动生成。→ 需要人工修改模板或手动编辑生成的用例。→ **导致用例生成方式缺乏灵活性，难以适应复杂多变的测试场景需求。**

### 5.3 用例组合依赖预设规则导致覆盖范围有限

**推理过程：**
对比技术通过预设组合规则对更新后测试用例进行组合处理（说明书第 [0156]-[0157] 段），在有场景测试用例时直接组合，无场景测试用例时获取预设测试用例进行补充。→ 组合逻辑完全依赖于预设规则。→ 当业务流程发生变化或出现新的业务场景时，预设规则可能无法正确识别用例间的依赖关系。→ 导致生成的组合测试用例可能遗漏关键的测试路径或产生冗余的测试用例。→ **导致测试覆盖范围受限，可能无法发现潜在的业务逻辑缺陷。**

### 5.4 调试过程依赖静态和动态规则导致智能化程度不足

**推理过程：**
对比技术通过静态调试指令和动态调试指令对组合测试用例进行调试（说明书第 [0140] 段、[0175] 段），根据用例依赖关系确定配置参数后进行调试。→ 调试规则需要预先定义，包括执行顺序依赖、参数传递依赖、资源依赖等。→ 当遇到未预定义的新型依赖关系或异常场景时，调试规则无法自动推导正确的处理方式。→ 需要人工分析调试失败原因并调整配置。→ **导致调试过程智能化程度不足，无法应对复杂的依赖关系和异常情况。**

### 5.5 缺乏页面结构理解能力导致跨页面测试困难

**推理过程：**
对比技术基于 HAR 文件解析生成接口测试用例（说明书第 [0159]-[0164] 段），关注点在于请求文件和接口参数的处理。→ 测试用例的生成和执行主要围绕接口层面，缺乏对网页 DOM 结构的理解和分析。→ 当测试任务涉及多个页面的跳转和交互时，无法自动识别页面元素之间的关联关系。→ 需要人工定义页面跳转逻辑和元素定位方式。→ **导致跨页面测试困难，无法自动处理复杂的用户界面交互场景。**

### 5.6 缺乏执行过程中的自适应调整能力

**推理过程：**
对比技术的测试用例在执行前需要经过调试处理，调试通过后才发送至服务端执行（说明书第 [0141]-[0145] 段）。→ 测试用例的执行逻辑在调试通过后即固定下来。→ 当执行过程中遇到页面变化、网络异常或数据变化等情况时，无法根据实时状态调整执行策略。→ 需要人工重新调试和修改用例。→ **导致测试执行过程缺乏自适应调整能力，面对变化时容易失败。**

### 5.7 脚本生成依赖固定转换规则导致可维护性不足

**推理过程：**
对比技术将调试通过的测试用例添加到待执行集合中（说明书第 [0198] 段），执行服务调用 JMeter 执行测试。→ 测试脚本的生成依赖于固定的转换规则，将用例信息转换为 JMeter 可执行的格式。→ 当需要更换测试执行引擎或适配不同的浏览器自动化框架时，需要修改转换规则或重新开发适配层。→ **导致脚本生成依赖固定转换规则，可移植性和可维护性不足。**

---

## 6. 本发明技术方案的详细阐述

**（本部分由发明人已提供，以下内容直接引用）**

### 6.1 系统整体架构

本发明提供了一种融合 DOM 语义解析与智能决策的测试自动化执行系统，如图 1 所示，系统 100 包括输入模块 101、处理模块 102、执行模块 103、输出模块 104 和网络监控模块 105。

**输入模块 101**：用于接收用户输入的测试任务描述，具体地，接收用户以自然语言形式描述的测试任务，例如"打开某银行网站首页，点击导航栏中的基金链接，在产品筛选区域选择投资类型为股票型、风险等级为 R4 中高风险，查询符合条件的基金数量"。输入模块还负责解析用户输入中的关键信息，包括目标 URL、期望执行的操作类型、预期的操作结果等。

**处理模块 102**：是本发明的核心模块，包括语义解析器、决策引擎和状态跟踪器。语义解析器负责对网页 DOM 元素进行抽象映射，生成具有语义信息的元素引用标识；决策引擎负责根据当前页面状态和任务意图通过推理 - 行动交替循环架构选择最优操作；状态跟踪器负责记录操作序列及页面状态变化并维护任务执行进度。

**执行模块 103**：用于根据处理模块生成的操作指令驱动浏览器完成页面交互，包括浏览器驱动、元素定位器和动作执行器。浏览器驱动采用 Playwright 浏览器自动化框架，支持 Chromium、Firefox、WebKit 等多种浏览器内核。

**网络监控模块 105**：用于在执行每个页面操作时，通过浏览器驱动的拦截机制捕获当前页面产生的所有网络请求，将网络请求信息与当前执行步骤的操作记录进行关联存储，形成操作 - 请求对应关系表，并基于操作 - 请求对应关系表生成前端接口性能分析报告。

**输出模块 104**：用于在任务完成后输出测试脚本和执行日志，将整个执行过程中记录的操作序列进行后处理，生成标准化的 JavaScript 测试脚本，同时输出详细的执行日志和性能分析报告。

### 6.2 方法流程

参考图 2，本发明提供的测试自动化执行方法采用 ReAct（Reasoning-Acting，推理 - 行动）架构，通过推理和行动的交替循环逐步完成任务，包括以下步骤：

**步骤 S201：开始**
系统初始化，加载必要的配置信息和浏览器驱动，包括 MCP 服务器配置、浏览器类型配置、最大迭代次数配置、上下文压缩阈值配置等。

**步骤 S202：加载目标网页**
执行模块通过浏览器驱动访问目标 URL，加载待测试的网页，监听页面加载事件，当页面达到稳定状态后通知后续步骤开始执行。

**步骤 S203：构建页面语义表示**
语义解析器遍历加载页面的 DOM 树结构，提取每个可见元素的属性信息，包括元素类型、文本内容、层级路径、可操作状态等，为每个可操作元素分配唯一的引用标识，建立 DOM 元素与引用标识之间的映射关系，生成当前页面的语义表示快照。

如图 3 所示，本发明通过三层结构实现 DOM 语义映射与操作定位：
- **网页 DOM 树 301**：包括 html 根节点、body 节点以及多个子元素节点
- **语义映射层 302**：将 DOM 树中的每个可操作元素映射为语义化的引用标识
- **操作抽象层 303**：基于引用标识，将用户操作抽象为标准化的动作指令

**步骤 S204：调用大语言模型进行推理**
处理模块构建包含系统提示词、对话历史和当前页面状态的提示消息，调用大语言模型生成推理响应，推理响应采用结构化格式，包括思考、步骤简称、动作和动作输入四个字段。

**步骤 S205：解析 AI 响应**
处理模块解析大语言模型返回的推理响应，提取动作名称和动作参数，验证动作名称是否存在于可用工具列表中。

**步骤 S206：判断是否为完成指令**
处理模块检查解析出的动作名称是否为完成指令，如果是则提取最终答案并返回任务执行结果，否则继续执行下一步骤。

**步骤 S207：调用工具执行操作**
处理模块从工具注册表中查找动作名称对应的工具对象，调用工具的执行方法，在浏览器环境中执行具体的页面操作，包括点击元素、输入文本、选择选项、滚动页面、等待时间等。

**步骤 S208：获取观察结果和页面快照**
处理模块接收工具执行返回的观察结果，可选地获取当前页面的语义表示快照，用于后续的元素定位和状态比较。

**步骤 S209：将观察结果追加到对话历史，进入下一轮 ReAct 循环**
处理模块将观察结果添加到对话历史列表中，检查当前循环次数是否达到最大迭代次数，如未达到则返回步骤 S204 继续下一轮循环。

**步骤 S210：结束**
任务执行完成，输出模块生成测试脚本和执行日志，对操作序列进行后处理，生成标准化的测试脚本并输出。

### 6.3 上下文压缩机制

在长时间任务执行过程中，对话历史记录会占用大量 token，上下文压缩器维护对话历史记录，当对话轮数达到预设阈值时自动触发压缩操作。压缩策略包括：
- 保留系统消息：系统提示词始终保留，不参与压缩
- 保留最近对话：保留最近 N 轮对话，默认 3 轮
- 压缩中间历史：将早期对话历史压缩为摘要信息
- 提取关键信息：通过正则表达式识别和提取对话中的关键信息

### 6.4 网络请求监控与性能分析

如图 4 所示，网络请求监控机制包括请求拦截子模块 401、信息提取子模块 402、关联存储子模块 403 和性能分析报告子模块 404。

**请求拦截子模块 401**：通过浏览器驱动提供的请求拦截 API 捕获所有 HTTP 和 HTTPS 请求。

**信息提取子模块 402**：从每个网络请求中提取请求 URL、请求方法、请求头、请求体、响应状态码、响应头、响应体以及请求耗时。

**关联存储子模块 403**：将网络请求信息与当前执行步骤的操作记录进行关联存储，形成操作 - 请求对应关系表。

**性能分析报告子模块 404**：在任务执行完成后基于操作 - 请求对应关系表生成前端接口性能分析报告，包括各接口的平均响应时间、最大响应时间、最小响应时间、请求成功率统计信息以及慢接口排行。

---

## 7. 本发明优点推导

本部分以因果关系推理的方式，逐层推导本发明相对于现有技术的技术效果。

### 7.1 降低脚本维护成本

**推理过程：**
本发明通过 DOM 语义解析为每个页面元素分配唯一的引用标识（说明书第 [0076] 段），建立 DOM 元素与引用标识之间的映射关系。→ 测试脚本中的操作基于语义化的引用标识，而非具体的 XPath 或 CSS 路径表达式。→ 当页面结构发生变化时，例如元素的 DOM 路径改变，系统只需重新遍历 DOM 树并更新映射关系。→ 引用标识保持不变，已生成的测试脚本无需修改即可继续执行。→ 无需人工介入修改脚本中的元素定位表达式。→ **直接技术效果：脚本维护工作量显著减少。** → 对于大型 Web 应用，页面数量众多，每次迭代可能涉及大量页面变更，人工修改脚本的成本成倍降低。→ **最终技术效果：显著降低测试脚本的维护成本，提高测试效率。**

### 7.2 提高测试适应性

**推理过程：**
本发明能够根据自然语言描述的任务自动推导执行步骤（说明书第 [0078]-[0088] 段），通过 ReAct 架构进行推理和行动的交替循环。→ 大语言模型根据当前页面状态和任务意图，动态决策下一步操作，而非按照预设的固定脚本执行。→ 同一任务描述在不同版本的页面结构中，智能体可以根据实际页面状态选择合适的元素进行操作。→ 在不同浏览器环境中，由于页面语义表示是统一的抽象层，智能体可以自适应不同的渲染结果。→ **直接技术效果：测试任务可适应不同版本的页面结构和不同浏览器环境。** → 测试人员无需为每个版本或每个浏览器编写不同的测试脚本。→ **最终技术效果：具有良好的跨平台适应性，减少测试脚本的重复编写工作。**

### 7.3 扩展测试覆盖范围

**推理过程：**
本发明降低了脚本编写和维护的门槛（说明书第 [0041] 段），测试人员只需以自然语言描述测试任务即可自动执行。→ 测试人员可以将精力集中在测试场景设计上，而非脚本实现细节上。→ 可以更轻松地创建大量测试用例，覆盖更多业务场景和边界条件。→ 同时，智能决策机制能够探索人工可能忽略的操作路径，例如在 ReAct 循环中，智能体可能根据页面状态推导出人意料的测试步骤。→ **直接技术效果：测试用例数量增加，测试路径更加丰富。** → 更多的测试用例和测试路径意味着更多的缺陷被发现的可能性。→ **最终技术效果：扩展测试覆盖范围，提高软件质量保障能力。**

### 7.4 提升测试效率

**推理过程：**
本发明将测试任务的抽象描述直接转换为可执行操作（说明书第 [0041] 段），省去了传统方法中脚本编写、调试、维护的时间。→ 测试人员无需学习复杂的测试框架和编程语法，只需以自然语言描述任务。→ 智能体自动完成页面加载、元素定位、操作执行、结果验证等步骤。→ 执行过程中自动处理异常情况，例如元素未找到时尝试其他定位策略。→ **直接技术效果：测试用例创建和执行时间大幅缩短。** → 对于频繁迭代的开发模式，测试人员可以快速响应变更，及时执行测试。→ **最终技术效果：提升测试效率，加快软件交付速度。**

### 7.5 支持回归测试

**推理过程：**
本发明自动生成的测试脚本基于语义化元素引用（说明书第 [0090] 段），可在后续版本中重复执行。→ 脚本中的元素定位基于引用标识，而非易变的 DOM 路径。→ 当页面结构发生小幅变更时，引用标识仍然有效，脚本可以正常执行。→ 相比传统脚本，回归测试过程中的误报（即因元素定位失败导致的假阴性）减少。→ **直接技术效果：回归测试脚本的稳定性提高。** → 测试人员无需在每次回归测试前花费大量时间修复失效的脚本。→ **最终技术效果：支持高效回归测试，减少误报，提高测试结果的可信度。**

### 7.6 提供性能分析能力

**推理过程：**
本发明通过网络监控模块捕获执行过程中的所有网络请求（说明书第 [0096] 段、[0137] 段），包括请求 URL、方法、请求头、响应状态码、响应体以及请求耗时。→ 网络请求信息与当前执行步骤的操作记录进行关联存储，形成操作 - 请求对应关系表。→ 任务执行完成后，基于对应关系表进行统计分析，生成各接口的平均响应时间、最大响应时间、最小响应时间、请求成功率等指标。→ 测试人员在验证功能的同时，可获得前端接口的性能数据。→ **直接技术效果：功能测试与性能测试同步完成。** → 开发人员可以根据性能分析报告识别响应时间过长的接口、频繁失败的请求等性能瓶颈。→ **最终技术效果：提供前端接口性能分析能力，支持应用性能优化。**

### 7.7 支持长时间复杂任务执行

**推理过程：**
本发明通过上下文压缩机制动态维护执行历史上下文（说明书第 [0094] 段），当对话历史长度超过预设阈值时自动触发压缩。→ 保留最近 N 轮对话和关键的推理 - 行动 - 观察三元组，压缩早期对话为摘要信息。→ 有效减少 token 消耗，避免因上下文超长导致的模型调用失败。→ 智能体可以基于压缩后的上下文继续推理，支持更长时间的任务执行。→ **直接技术效果：支持涉及多页面、多步骤的复杂测试任务。** → 对于大型业务流程测试，例如跨数十个页面的完整业务闭环，智能体可以持续执行直至任务完成。→ **最终技术效果：提高复杂测试场景的自动化执行能力。**

---

## 8. 本发明的关键点和欲保护点

**（本部分由发明人已提供，以下内容直接引用）**

本发明提炼出以下关键创新点：

1. **DOM 语义解析与引用标识映射机制**：遍历网页 DOM 树结构，为每个可见元素提取属性信息并分配唯一的语义化引用标识，建立 DOM 元素与引用标识之间的映射关系，使测试脚本不依赖于具体的 DOM 路径表达式。

2. **基于 ReAct 架构的推理 - 行动循环执行机制**：采用推理 - 行动交替循环架构，将当前页面语义表示和对话历史构建为提示消息，调用大语言模型生成推理响应，解析后执行相应的页面操作，获取观察结果并进入下一轮循环，直至任务完成。

3. **自然语言任务描述到可执行操作的自动转换**：接收用户以自然语言描述的测试任务，通过语义分析提取关键实体和操作意图，自动推导执行步骤和操作序列，无需预先编写测试脚本。

4. **上下文压缩机制**：在长时间任务执行过程中，动态维护执行历史上下文，当对话历史长度超过预设阈值时自动触发压缩，保留最近 N 轮对话和关键信息，减少 token 消耗并支持更长时间的任务执行。

5. **网络请求监控与性能分析机制**：在执行每个页面操作时，通过浏览器驱动的拦截机制捕获所有网络请求，将网络请求信息与当前执行步骤的操作记录进行关联存储，生成前端接口性能分析报告。

6. **标准化测试脚本自动生成**：将整个执行过程中记录的操作序列进行后处理，去除冗余操作、合并连续操作、优化元素定位表达式，生成适配主流浏览器自动化框架的标准化测试脚本。

7. **MCP 协议支持的工具动态扩展**：通过 MCP（Model Context Protocol）协议接入外部工具服务，根据配置文件动态加载浏览器自动化、上下文管理等工具，无需修改代码即可扩展智能体能力。

---

## 9. 替代方案

经发明人分析，针对本发明上述技术方案，目前**没有其他替代方案**能够同样完成发明目的。

本发明采用的 DOM 语义解析与引用标识映射机制、基于 ReAct 架构的推理 - 行动循环执行机制、自然语言任务描述到可执行操作的自动转换等核心技术特征，是实现无需预先编写测试脚本即可完成复杂测试场景自动化执行的必要技术手段。任何其他技术方案若要实现相同的发明目的，均需采用与本发明实质上相同的技术手段。

---

## 10. 其他有助于专利代理人理解技术的资料

无。

---

## 附图说明

本发明提供的附图如下（以黑白线条图形式提供）：

- **图 1**：系统整体架构示意图
- **图 2**：基于 ReAct 架构的测试执行流程示意图
- **图 3**：DOM 语义映射与操作定位示意图
- **图 4**：网络请求监控与性能分析示意图

（附图内容参见 `patent/figures.md` 文件）

---

**交底书撰写完成**
